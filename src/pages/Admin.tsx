import { products } from "../data/products"; export default function Admin(){return <section className="container py-14"><p className="text-xs uppercase tracking-widest text-champagne">Management</p><h1 className="font-display text-5xl">Admin Dashboard</h1><div className="mt-10 grid gap-4 md:grid-cols-4">{[["Products",products.length],["Orders",0],["Customers",0],["Sales","Rs. 0"]].map(([a,b])=><div className="card p-6" key={String(a)}><p className="text-sm text-stone-500">{a}</p><p className="mt-2 text-3xl font-semibold">{b}</p></div>)}</div><p className="mt-8 text-sm text-stone-500">This dashboard is the UI foundation. Supabase admin CRUD can be enabled after the included schema is applied.</p></section>}